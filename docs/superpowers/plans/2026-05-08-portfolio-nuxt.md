# Portfolio Nuxt — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build Miguel Angel Escamilla's full-stack portfolio as a one-page Nuxt 4 SSG site (ES/EN, dark theme, Bootstrap 5 SCSS), deployable to GitHub Pages at `mig21010.github.io`.

**Architecture:** Single page (`pages/index.vue`) with three sections (Hero, About, Work). Bilingual via `@nuxtjs/i18n` (`prefix` strategy). Structured data in `app/content/*.ts` files; UI text in `i18n/locales/*.json`. Bootstrap 5 SCSS imported partially (grid + reboot + utilities only). Static build via `nuxt generate` (nitro `github-pages` preset), deployed by GitHub Actions workflow.

**Tech Stack:** Nuxt 4, Vue 3.5+, TypeScript, Bootstrap 5.3 (SCSS), Bootstrap Icons, `@nuxtjs/i18n` v9, SCSS, Vitest + `@vue/test-utils`.

---

## File Structure

**To create:**
```
package.json
nuxt.config.ts
tsconfig.json
vitest.config.ts
.gitignore
.editorconfig
README.md
app/
├── app.vue
├── pages/index.vue
├── components/
│   ├── layout/SiteHeader.vue
│   ├── layout/SiteFooter.vue
│   ├── sections/HeroSection.vue
│   ├── sections/AboutSection.vue
│   ├── sections/WorkSection.vue
│   └── ui/SocialLink.vue
│   └── ui/SkillGroup.vue
│   └── ui/LanguageSwitcher.vue
├── content/types.ts
├── content/socials.ts
├── content/skills.ts
├── content/work.ts
└── utils/experience.ts
i18n/locales/es.json
i18n/locales/en.json
assets/scss/main.scss
assets/scss/_variables.scss
assets/scss/_typography.scss
assets/scss/_components.scss
public/images/photo-placeholder.svg
public/favicon.svg
tests/setup.ts
tests/utils/experience.test.ts
tests/components/LanguageSwitcher.test.ts
tests/components/SkillGroup.test.ts
tests/components/WorkSection.test.ts
.github/workflows/deploy.yml
```

---

## Task 1: Initialize Nuxt 4 project

**Files:**
- Create: `package.json`, `nuxt.config.ts`, `tsconfig.json`, `app/app.vue`, `.gitignore`, `.editorconfig`

- [ ] **Step 1: Create `package.json`**

```json
{
  "name": "my-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "nuxt": "^4.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@nuxtjs/i18n": "^9.0.0",
    "@vue/test-utils": "^2.4.6",
    "bootstrap": "^5.3.3",
    "bootstrap-icons": "^1.11.3",
    "happy-dom": "^15.0.0",
    "sass": "^1.79.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "vue-tsc": "^2.1.0"
  }
}
```

- [ ] **Step 2: Create `tsconfig.json`**

```json
{
  "extends": "./.nuxt/tsconfig.json"
}
```

- [ ] **Step 3: Create `.gitignore`**

```
# Nuxt
.nuxt
.output
.data
node_modules
.env
.DS_Store
dist
*.log

# OS
Thumbs.db

# IDE
.vscode
.idea
```

- [ ] **Step 4: Create `.editorconfig`**

```
root = true

[*]
indent_style = space
indent_size = 2
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true
```

- [ ] **Step 5: Create minimal `nuxt.config.ts`**

```ts
export default defineNuxtConfig({
  compatibilityDate: '2026-05-08',
  devtools: { enabled: true },
  future: { compatibilityVersion: 4 },
  typescript: { strict: true },
  app: {
    baseURL: '/',
    head: {
      title: 'Miguel Angel Escamilla — Full-stack Developer',
      htmlAttrs: { lang: 'es' },
      meta: [
        { name: 'viewport', content: 'width=device-width, initial-scale=1' },
        { name: 'description', content: 'Full-stack developer portfolio.' },
      ],
    },
  },
})
```

- [ ] **Step 6: Create minimal `app/app.vue`**

```vue
<template>
  <NuxtPage />
</template>
```

- [ ] **Step 7: Create placeholder `app/pages/index.vue`**

```vue
<template>
  <main>
    <h1>Portfolio</h1>
  </main>
</template>
```

- [ ] **Step 8: Install dependencies**

Run: `npm install`
Expected: install completes, `node_modules/` populated, `.nuxt/` generated by `postinstall`.

- [ ] **Step 9: Verify dev server starts**

Run: `npm run dev` (let it boot ~10s, then Ctrl-C)
Expected: server listening on http://localhost:3000, no errors.

- [ ] **Step 10: Commit**

```bash
git add package.json package-lock.json nuxt.config.ts tsconfig.json .gitignore .editorconfig app/
git commit -m "feat: bootstrap Nuxt 4 project scaffold"
```

---

## Task 2: Configure SCSS architecture (without Bootstrap yet)

**Files:**
- Create: `assets/scss/main.scss`, `assets/scss/_variables.scss`, `assets/scss/_typography.scss`, `assets/scss/_components.scss`
- Modify: `nuxt.config.ts` (register CSS entry)

- [ ] **Step 1: Create `assets/scss/_variables.scss`**

```scss
// Palette (dark theme)
$bg-primary:    #0a0a0a;
$bg-elevated:   #1a1a1a;
$text-primary:  #ffffff;
$text-muted:    #888888;
$accent:        #ffffff;
$border-subtle: rgba(255, 255, 255, 0.12);

// Bootstrap variable overrides (these MUST be defined before Bootstrap imports)
$body-bg:                $bg-primary;
$body-color:             $text-primary;
$border-radius:          1rem;
$border-radius-lg:       2rem;
$font-family-sans-serif: 'Inter', system-ui, -apple-system, sans-serif;
$font-family-display:    'Archivo Black', 'Inter', sans-serif;
```

- [ ] **Step 2: Create `assets/scss/_typography.scss`**

```scss
:root {
  font-family: $font-family-sans-serif;
  color: $text-primary;
  background: $bg-primary;
}

body {
  font-size: 1rem;
  line-height: 1.6;
  scroll-behavior: smooth;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

.display-hero {
  font-family: $font-family-display;
  font-size: clamp(3rem, 12vw, 8rem);
  line-height: 0.9;
  letter-spacing: -0.02em;
}

.display-section {
  font-family: $font-family-display;
  font-size: clamp(2.5rem, 8vw, 5.5rem);
  line-height: 0.9;
}

.section-label {
  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
  font-size: 0.875rem;
  color: $text-muted;
  letter-spacing: 0.05em;
}
```

- [ ] **Step 3: Create `assets/scss/_components.scss`**

```scss
.btn-pill {
  display: inline-flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.75rem 1.75rem;
  border: 1px solid $border-subtle;
  border-radius: 999px;
  background: transparent;
  color: $text-primary;
  text-decoration: none;
  transition: background 150ms ease, border-color 150ms ease;

  &:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: $text-primary;
    color: $text-primary;
  }
}

.btn-pill--solid {
  background: $accent;
  color: $bg-primary;

  &:hover {
    background: $text-primary;
    color: $bg-primary;
  }
}

.skill-group {
  border: 1px solid $border-subtle;
  border-radius: $border-radius-lg;
  padding: 1.5rem 2rem;
  background: rgba(255, 255, 255, 0.02);

  &__title {
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: $text-primary;
  }

  &__items {
    color: $text-muted;
    font-size: 0.95rem;
    line-height: 1.7;
  }
}

.work-table {
  width: 100%;
  border-collapse: collapse;

  td {
    padding: 1rem 1.25rem;
    border-top: 1px solid $border-subtle;
    vertical-align: middle;
  }

  tr:last-child td { border-bottom: 1px solid $border-subtle; }
}

.work-row--highlight {
  background: $accent;
  color: $bg-primary;

  td { color: $bg-primary; }
}

.social-link {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 1rem;
  border: 1px solid $border-subtle;
  border-radius: 999px;
  color: $text-primary;
  text-decoration: none;
  font-size: 0.875rem;
  transition: background 150ms ease;

  &:hover { background: rgba(255, 255, 255, 0.08); color: $text-primary; }

  i { font-size: 1rem; }
}

.lang-switcher {
  display: inline-flex;
  flex-direction: column;
  font-size: 0.875rem;

  a {
    color: $text-muted;
    text-decoration: none;
    line-height: 1.4;

    &.active { color: $text-primary; font-weight: 600; }
    &:hover  { color: $text-primary; }
  }
}
```

- [ ] **Step 4: Create `assets/scss/main.scss`**

```scss
@import 'variables';
@import 'typography';
@import 'components';
```

- [ ] **Step 5: Register CSS entry in `nuxt.config.ts`**

Replace the `nuxt.config.ts` from Task 1 with:

```ts
export default defineNuxtConfig({
  compatibilityDate: '2026-05-08',
  devtools: { enabled: true },
  future: { compatibilityVersion: 4 },
  typescript: { strict: true },
  css: ['~/assets/scss/main.scss'],
  vite: {
    css: {
      preprocessorOptions: {
        scss: {
          additionalData: '@use "~/assets/scss/variables" as *;',
        },
      },
    },
  },
  app: {
    baseURL: '/',
    head: {
      title: 'Miguel Angel Escamilla — Full-stack Developer',
      htmlAttrs: { lang: 'es' },
      meta: [
        { name: 'viewport', content: 'width=device-width, initial-scale=1' },
        { name: 'description', content: 'Full-stack developer portfolio.' },
      ],
    },
  },
})
```

> **Note:** `additionalData` injects `_variables.scss` into every SCSS file/SFC `<style lang="scss">` so component styles can reference `$bg-primary` etc. without manual imports.

- [ ] **Step 6: Verify dev server still works with SCSS loaded**

Run: `npm run dev` (boot ~10s, Ctrl-C)
Expected: no SCSS errors in console; visiting `/` should show the page with white text on dark background.

- [ ] **Step 7: Commit**

```bash
git add assets/ nuxt.config.ts
git commit -m "feat: add SCSS architecture with dark theme variables"
```

---

## Task 3: Add Bootstrap 5 SCSS (partial)

**Files:**
- Modify: `assets/scss/main.scss`

- [ ] **Step 1: Update `assets/scss/main.scss` to import Bootstrap parts + Bootstrap Icons**

```scss
// 1. Custom variable overrides FIRST (so Bootstrap picks them up)
@import 'variables';

// 2. Bootstrap (only what we need)
@import 'bootstrap/scss/functions';
@import 'bootstrap/scss/variables';
@import 'bootstrap/scss/variables-dark';
@import 'bootstrap/scss/maps';
@import 'bootstrap/scss/mixins';
@import 'bootstrap/scss/utilities';
@import 'bootstrap/scss/root';
@import 'bootstrap/scss/reboot';
@import 'bootstrap/scss/containers';
@import 'bootstrap/scss/grid';
@import 'bootstrap/scss/utilities/api';

// 3. Bootstrap Icons font
@import 'bootstrap-icons/font/bootstrap-icons.css';

// 4. Custom typography and components last
@import 'typography';
@import 'components';
```

- [ ] **Step 2: Verify build still works**

Run: `npm run dev` (boot ~10s, Ctrl-C)
Expected: no SCSS compile errors. The `.container`, `.row`, `.col-*` classes from Bootstrap are now available.

- [ ] **Step 3: Commit**

```bash
git add assets/scss/main.scss
git commit -m "feat: integrate Bootstrap 5 SCSS partial + Bootstrap Icons"
```

---

## Task 4: Add Inter + Archivo Black fonts

**Files:**
- Modify: `nuxt.config.ts` (add font preload + stylesheet links)

- [ ] **Step 1: Add font links to `nuxt.config.ts` `app.head`**

Replace the `app.head` block in `nuxt.config.ts` with:

```ts
app: {
  baseURL: '/',
  head: {
    title: 'Miguel Angel Escamilla — Full-stack Developer',
    htmlAttrs: { lang: 'es' },
    meta: [
      { name: 'viewport', content: 'width=device-width, initial-scale=1' },
      { name: 'description', content: 'Full-stack developer portfolio.' },
    ],
    link: [
      { rel: 'preconnect', href: 'https://fonts.googleapis.com' },
      { rel: 'preconnect', href: 'https://fonts.gstatic.com', crossorigin: '' },
      {
        rel: 'stylesheet',
        href: 'https://fonts.googleapis.com/css2?family=Archivo+Black&family=Inter:wght@400;500;600;700&display=swap',
      },
    ],
  },
},
```

- [ ] **Step 2: Verify fonts load**

Run: `npm run dev`. Open `http://localhost:3000` in a browser. Inspect: the body text should render in Inter (not the fallback). Ctrl-C to stop.

- [ ] **Step 3: Commit**

```bash
git add nuxt.config.ts
git commit -m "feat: add Inter + Archivo Black font loading"
```

---

## Task 5: Configure i18n (ES / EN)

**Files:**
- Create: `i18n/locales/es.json`, `i18n/locales/en.json`
- Modify: `nuxt.config.ts` (register `@nuxtjs/i18n` module)

- [ ] **Step 1: Create `i18n/locales/es.json`**

```json
{
  "nav": {
    "about": "Sobre mí",
    "work": "Experiencia"
  },
  "hero": {
    "title": "Full-stack Developer",
    "intro": "Mi objetivo es escribir código mantenible, claro y comprensible para que el desarrollo sea disfrutable.",
    "cta": "Proyectos"
  },
  "about": {
    "label": "/Sobre mí",
    "intro": "Hola, soy Miguel Angel, full-stack developer con más de 5 años de experiencia.",
    "skillsTitle": {
      "frontend": "Front-end",
      "styles": "Estilos",
      "backend": "Back-end",
      "devops": "DevOps"
    },
    "photoAlt": "Foto de Miguel Angel Escamilla"
  },
  "work": {
    "title": "Experiencia",
    "totalLabel": "Experiencia laboral",
    "yearsMonths": "{years} años {months} meses",
    "present": "Actual",
    "duration": "{years}a {months}m",
    "jobs": {
      "job-1": { "role": "Full-stack developer | Vue & Node" },
      "job-2": { "role": "Frontend developer | React" }
    }
  },
  "footer": {
    "copyright": "© {year} Miguel Angel Escamilla"
  }
}
```

- [ ] **Step 2: Create `i18n/locales/en.json`**

```json
{
  "nav": {
    "about": "About",
    "work": "Work"
  },
  "hero": {
    "title": "Full-stack Developer",
    "intro": "My goal is to write maintainable, clean and understandable code so development is enjoyable.",
    "cta": "Projects"
  },
  "about": {
    "label": "/About me",
    "intro": "Hello! I'm Miguel Angel, a full-stack developer with more than 5 years of experience.",
    "skillsTitle": {
      "frontend": "Front-end",
      "styles": "Styles",
      "backend": "Back-end",
      "devops": "DevOps"
    },
    "photoAlt": "Photo of Miguel Angel Escamilla"
  },
  "work": {
    "title": "Work",
    "totalLabel": "Work experience",
    "yearsMonths": "{years} years {months} months",
    "present": "Present",
    "duration": "{years}y {months}m",
    "jobs": {
      "job-1": { "role": "Full-stack developer | Vue & Node" },
      "job-2": { "role": "Frontend developer | React" }
    }
  },
  "footer": {
    "copyright": "© {year} Miguel Angel Escamilla"
  }
}
```

- [ ] **Step 3: Register i18n module in `nuxt.config.ts`**

Add `modules: ['@nuxtjs/i18n']` and an `i18n: {...}` block to `defineNuxtConfig({...})`. The complete `nuxt.config.ts` should now look like:

```ts
export default defineNuxtConfig({
  compatibilityDate: '2026-05-08',
  devtools: { enabled: true },
  future: { compatibilityVersion: 4 },
  typescript: { strict: true },
  modules: ['@nuxtjs/i18n'],
  css: ['~/assets/scss/main.scss'],
  vite: {
    css: {
      preprocessorOptions: {
        scss: {
          additionalData: '@use "~/assets/scss/variables" as *;',
        },
      },
    },
  },
  i18n: {
    strategy: 'prefix',
    defaultLocale: 'es',
    locales: [
      { code: 'es', name: 'Español', file: 'es.json' },
      { code: 'en', name: 'English', file: 'en.json' },
    ],
    detectBrowserLanguage: false,
  },
  app: {
    baseURL: '/',
    head: {
      title: 'Miguel Angel Escamilla — Full-stack Developer',
      htmlAttrs: { lang: 'es' },
      meta: [
        { name: 'viewport', content: 'width=device-width, initial-scale=1' },
        { name: 'description', content: 'Full-stack developer portfolio.' },
      ],
      link: [
        { rel: 'preconnect', href: 'https://fonts.googleapis.com' },
        { rel: 'preconnect', href: 'https://fonts.gstatic.com', crossorigin: '' },
        {
          rel: 'stylesheet',
          href: 'https://fonts.googleapis.com/css2?family=Archivo+Black&family=Inter:wght@400;500;600;700&display=swap',
        },
      ],
    },
  },
})
```

- [ ] **Step 4: Verify i18n loaded**

Run: `npm run dev`. Visit `http://localhost:3000/es` and `http://localhost:3000/en`. Both should render the placeholder page (no errors). Visiting `http://localhost:3000/` should redirect to `/es`. Ctrl-C.

- [ ] **Step 5: Commit**

```bash
git add i18n/ nuxt.config.ts
git commit -m "feat: configure @nuxtjs/i18n with ES/EN prefix strategy"
```

---

## Task 6: Configure Vitest

**Files:**
- Create: `vitest.config.ts`, `tests/setup.ts`

- [ ] **Step 1: Create `vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config'
import vue from '@vitejs/plugin-vue'
import { fileURLToPath } from 'node:url'

export default defineConfig({
  plugins: [vue()],
  test: {
    globals: true,
    environment: 'happy-dom',
    setupFiles: ['./tests/setup.ts'],
  },
  resolve: {
    alias: {
      '~': fileURLToPath(new URL('./', import.meta.url)),
      '@': fileURLToPath(new URL('./', import.meta.url)),
    },
  },
})
```

- [ ] **Step 2: Add `@vitejs/plugin-vue` to dev deps**

Edit `package.json` `devDependencies` — add the line `"@vitejs/plugin-vue": "^5.1.0",` (alphabetically between `@nuxtjs/i18n` and `@vue/test-utils`). Then run:

```bash
npm install
```

- [ ] **Step 3: Create `tests/setup.ts`**

```ts
import { vi } from 'vitest'
import { config } from '@vue/test-utils'

// Translation helper: returns the key, with {param} placeholders substituted.
const t = (key: string, params?: Record<string, unknown>) => {
  if (!params) return key
  return Object.entries(params).reduce(
    (acc, [k, v]) => acc.replaceAll(`{${k}}`, String(v)),
    key,
  )
}

// Stub Nuxt auto-imports as globals so components calling useI18n(), etc. work in tests.
vi.stubGlobal('useI18n', () => ({
  t,
  locale: { value: 'es' },
  locales: { value: [{ code: 'es', name: 'Español' }, { code: 'en', name: 'English' }] },
}))
vi.stubGlobal('useSwitchLocalePath', () => (code: string) => `/${code}`)
vi.stubGlobal('useHead', () => undefined)

// Stub <NuxtLink> so component tests don't need a Nuxt runtime.
config.global.stubs = {
  NuxtLink: {
    props: ['to'],
    template: '<a :href="typeof to === \'string\' ? to : (to?.path ?? \'#\')"><slot /></a>',
  },
}

// Provide $t for templates that use it instead of useI18n's t.
config.global.mocks = { $t: t }
```

- [ ] **Step 4: Verify Vitest runs (with no tests yet)**

Run: `npm test`
Expected: Vitest reports "No test files found" — that's OK, exits 0 or 1 depending on config; we just need it to not crash on setup.

- [ ] **Step 5: Commit**

```bash
git add vitest.config.ts tests/setup.ts package.json package-lock.json
git commit -m "test: configure Vitest + Vue Test Utils with NuxtLink stub"
```

---

## Task 7: Create shared types

**Files:**
- Create: `app/content/types.ts`

- [ ] **Step 1: Create `app/content/types.ts`**

```ts
export type Social = {
  id: 'github' | 'linkedin' | 'email' | 'twitter' | 'instagram'
  url: string
  icon: string
  label: string
}

export type SkillGroup = {
  id: 'frontend' | 'styles' | 'backend' | 'devops'
  items: string[]
}

export type Job = {
  id: string
  start: string
  end: string | 'present'
  company: string
  roleKey: string
}
```

- [ ] **Step 2: Verify typecheck passes**

Run: `npm run typecheck`
Expected: no errors (warnings about missing files are OK).

- [ ] **Step 3: Commit**

```bash
git add app/content/types.ts
git commit -m "feat: add shared content types"
```

---

## Task 8: Create static content modules

**Files:**
- Create: `app/content/socials.ts`, `app/content/skills.ts`, `app/content/work.ts`

- [ ] **Step 1: Create `app/content/socials.ts`**

```ts
import type { Social } from './types'

export const socials: Social[] = [
  { id: 'github',    url: 'https://github.com/mig21010',          icon: 'bi-github',    label: 'GitHub' },
  { id: 'email',     url: 'mailto:rodrigo.tejero@thewebchi.mp',   icon: 'bi-envelope',  label: 'Email' },
  { id: 'twitter',   url: 'https://twitter.com/PLACEHOLDER',      icon: 'bi-twitter-x', label: 'X' },
  { id: 'instagram', url: 'https://instagram.com/PLACEHOLDER',    icon: 'bi-instagram', label: 'Instagram' },
]
```

- [ ] **Step 2: Create `app/content/skills.ts`**

```ts
import type { SkillGroup } from './types'

export const skillGroups: SkillGroup[] = [
  { id: 'frontend', items: ['Vue', 'Nuxt', 'React', 'TypeScript', 'Pinia', 'Vite'] },
  { id: 'styles',   items: ['SCSS', 'SASS', 'Bootstrap', 'Tailwind', 'PostCSS'] },
  { id: 'backend',  items: ['Node', 'PHP', 'Laravel', 'MySQL', 'PostgreSQL', 'REST', 'GraphQL'] },
  { id: 'devops',   items: ['Docker', 'Nginx', 'GitHub Actions', 'Bash', 'Linux'] },
]
```

- [ ] **Step 3: Create `app/content/work.ts`**

```ts
import type { Job } from './types'

export const jobs: Job[] = [
  { id: 'job-1', start: '2024-01', end: 'present', company: 'PLACEHOLDER Co', roleKey: 'work.jobs.job-1.role' },
  { id: 'job-2', start: '2022-06', end: '2023-12', company: 'PLACEHOLDER Co', roleKey: 'work.jobs.job-2.role' },
]
```

- [ ] **Step 4: Verify typecheck**

Run: `npm run typecheck`
Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add app/content/
git commit -m "feat: add socials, skills, and work content modules"
```

---

## Task 9: Implement `calculateExperience` (TDD)

**Files:**
- Test: `tests/utils/experience.test.ts`
- Create: `app/utils/experience.ts`

- [ ] **Step 1: Write failing tests**

Create `tests/utils/experience.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { calculateExperience } from '~/app/utils/experience'
import type { Job } from '~/app/content/types'

const make = (start: string, end: string | 'present', id = 'j'): Job => ({
  id, start, end, company: 'X', roleKey: 'k',
})

describe('calculateExperience', () => {
  it('returns 0/0 for empty job list', () => {
    expect(calculateExperience([])).toEqual({ years: 0, months: 0 })
  })

  it('computes a single closed range exactly', () => {
    const jobs = [make('2022-01', '2023-01')]
    expect(calculateExperience(jobs)).toEqual({ years: 1, months: 0 })
  })

  it('counts months for partial-year ranges', () => {
    const jobs = [make('2022-01', '2022-07')]
    expect(calculateExperience(jobs)).toEqual({ years: 0, months: 6 })
  })

  it('treats "present" as the injected now date', () => {
    const jobs = [make('2024-01', 'present')]
    const now = new Date(2026, 4, 1) // May 2026 — month is 0-indexed
    expect(calculateExperience(jobs, now)).toEqual({ years: 2, months: 4 })
  })

  it('sums multiple job ranges', () => {
    const jobs = [
      make('2020-01', '2021-01', 'a'), // 12 months
      make('2021-06', '2022-06', 'b'), // 12 months
    ]
    expect(calculateExperience(jobs)).toEqual({ years: 2, months: 0 })
  })

  it('handles a same-month range as 0 months', () => {
    const jobs = [make('2024-03', '2024-03')]
    expect(calculateExperience(jobs)).toEqual({ years: 0, months: 0 })
  })
})
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `npm test`
Expected: FAIL — `calculateExperience` not found.

- [ ] **Step 3: Implement `app/utils/experience.ts`**

```ts
import type { Job } from '~/app/content/types'

export function calculateExperience(
  jobs: Job[],
  now: Date = new Date(),
): { years: number; months: number } {
  let totalMonths = 0

  for (const job of jobs) {
    const startDate = parseYearMonth(job.start)
    const endDate = job.end === 'present' ? now : parseYearMonth(job.end)
    const months =
      (endDate.getFullYear() - startDate.getFullYear()) * 12 +
      (endDate.getMonth() - startDate.getMonth())
    if (months > 0) totalMonths += months
  }

  return { years: Math.floor(totalMonths / 12), months: totalMonths % 12 }
}

function parseYearMonth(value: string): Date {
  const [year, month] = value.split('-').map(Number)
  return new Date(year, month - 1, 1)
}
```

- [ ] **Step 4: Run tests — verify they pass**

Run: `npm test`
Expected: PASS — all 6 `calculateExperience` tests green.

- [ ] **Step 5: Commit**

```bash
git add app/utils/experience.ts tests/utils/experience.test.ts
git commit -m "feat: add calculateExperience utility with tests"
```

---

## Task 10: Build `SocialLink` component

**Files:**
- Create: `app/components/ui/SocialLink.vue`

- [ ] **Step 1: Create `app/components/ui/SocialLink.vue`**

```vue
<script setup lang="ts">
defineProps<{
  icon: string
  url: string
  label: string
}>()
</script>

<template>
  <a
    class="social-link"
    :href="url"
    target="_blank"
    rel="noopener noreferrer"
  >
    <i :class="['bi', icon]" aria-hidden="true" />
    <span>{{ label }}</span>
  </a>
</template>
```

- [ ] **Step 2: Verify typecheck**

Run: `npm run typecheck`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add app/components/ui/SocialLink.vue
git commit -m "feat: add SocialLink component"
```

---

## Task 11: Build `SkillGroup` component (TDD)

**Files:**
- Test: `tests/components/SkillGroup.test.ts`
- Create: `app/components/ui/SkillGroup.vue`

- [ ] **Step 1: Write failing test**

Create `tests/components/SkillGroup.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import SkillGroup from '~/app/components/ui/SkillGroup.vue'

describe('SkillGroup', () => {
  it('renders the resolved title and items joined by a separator', () => {
    const wrapper = mount(SkillGroup, {
      props: {
        titleKey: 'about.skillsTitle.frontend',
        items: ['Vue', 'Nuxt', 'React'],
      },
    })

    expect(wrapper.text()).toContain('about.skillsTitle.frontend')
    const itemsText = wrapper.find('.skill-group__items').text()
    expect(itemsText).toContain('Vue')
    expect(itemsText).toContain('Nuxt')
    expect(itemsText).toContain('React')
  })

  it('renders all items when given a long list', () => {
    const items = ['A', 'B', 'C', 'D', 'E', 'F', 'G']
    const wrapper = mount(SkillGroup, {
      props: { titleKey: 'about.skillsTitle.backend', items },
    })

    for (const item of items) {
      expect(wrapper.find('.skill-group__items').text()).toContain(item)
    }
  })
})
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `npm test`
Expected: FAIL — `SkillGroup` not found.

- [ ] **Step 3: Create `app/components/ui/SkillGroup.vue`**

```vue
<script setup lang="ts">
defineProps<{
  titleKey: string
  items: string[]
}>()
</script>

<template>
  <div class="skill-group">
    <div class="skill-group__title">{{ $t(titleKey) }}</div>
    <div class="skill-group__items">{{ items.join(' / ') }}</div>
  </div>
</template>
```

- [ ] **Step 4: Run tests — verify they pass**

Run: `npm test`
Expected: PASS — both `SkillGroup` tests green.

- [ ] **Step 5: Commit**

```bash
git add app/components/ui/SkillGroup.vue tests/components/SkillGroup.test.ts
git commit -m "feat: add SkillGroup component with tests"
```

---

## Task 12: Build `LanguageSwitcher` component (TDD)

**Files:**
- Test: `tests/components/LanguageSwitcher.test.ts`
- Create: `app/components/ui/LanguageSwitcher.vue`

- [ ] **Step 1: Write failing test**

The component will delegate to a composable `useLocaleLinks` (defined in Step 3) that wraps Nuxt's `useI18n` + `useSwitchLocalePath`. The wrapper exists so tests can mock one symbol cleanly.

Create `tests/components/LanguageSwitcher.test.ts`:

```ts
import { describe, it, expect, vi } from 'vitest'
import { ref } from 'vue'
import { mount } from '@vue/test-utils'
import LanguageSwitcher from '~/app/components/ui/LanguageSwitcher.vue'

vi.mock('~/app/composables/useLocaleLinks', () => ({
  useLocaleLinks: () => ({
    currentLocale: ref('es'),
    links: ref([
      { code: 'en', label: 'En', path: '/en' },
      { code: 'es', label: 'Es', path: '/es' },
    ]),
  }),
}))

describe('LanguageSwitcher', () => {
  it('renders one link per locale with correct labels and hrefs', () => {
    const wrapper = mount(LanguageSwitcher)
    const links = wrapper.findAll('a')

    expect(links).toHaveLength(2)
    expect(links[0].text()).toBe('En')
    expect(links[0].attributes('href')).toBe('/en')
    expect(links[1].text()).toBe('Es')
    expect(links[1].attributes('href')).toBe('/es')
  })

  it('marks the current locale link with an active class', () => {
    const wrapper = mount(LanguageSwitcher)
    const links = wrapper.findAll('a')

    expect(links[1].classes()).toContain('active')
    expect(links[0].classes()).not.toContain('active')
  })
})
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `npm test`
Expected: FAIL — `LanguageSwitcher` and `useLocaleLinks` not found.

- [ ] **Step 3: Create `app/composables/useLocaleLinks.ts`**

```ts
import { computed } from 'vue'

export function useLocaleLinks() {
  const { locale, locales } = useI18n()
  const switchLocalePath = useSwitchLocalePath()

  const currentLocale = computed(() => locale.value)

  const links = computed(() =>
    (locales.value as Array<{ code: string; name: string }>).map((l) => ({
      code: l.code,
      label: l.code === 'es' ? 'Es' : 'En',
      path: switchLocalePath(l.code) || '/',
    })),
  )

  return { currentLocale, links }
}
```

- [ ] **Step 4: Create `app/components/ui/LanguageSwitcher.vue`**

```vue
<script setup lang="ts">
import { useLocaleLinks } from '~/app/composables/useLocaleLinks'

const { currentLocale, links } = useLocaleLinks()
</script>

<template>
  <div class="lang-switcher">
    <a
      v-for="link in links"
      :key="link.code"
      :href="link.path"
      :class="{ active: link.code === currentLocale }"
    >
      {{ link.label }}
    </a>
  </div>
</template>
```

> **Note:** The component reads from `useLocaleLinks`. In production, the composable uses Nuxt's auto-imported `useI18n` and `useSwitchLocalePath`. In tests, the entire composable is mocked (Step 1), so those auto-imports are never invoked.

- [ ] **Step 5: Run tests — verify they pass**

Run: `npm test`
Expected: PASS — both `LanguageSwitcher` tests green.

- [ ] **Step 6: Commit**

```bash
git add app/composables/ app/components/ui/LanguageSwitcher.vue tests/components/LanguageSwitcher.test.ts
git commit -m "feat: add LanguageSwitcher with mockable composable wrapper"
```

---

## Task 13: Build `SiteHeader` and `SiteFooter`

**Files:**
- Create: `app/components/layout/SiteHeader.vue`, `app/components/layout/SiteFooter.vue`

- [ ] **Step 1: Create `app/components/layout/SiteHeader.vue`**

```vue
<script setup lang="ts">
import LanguageSwitcher from '~/app/components/ui/LanguageSwitcher.vue'

const { t } = useI18n()
</script>

<template>
  <header class="site-header">
    <div class="container py-4">
      <div class="d-flex justify-content-between align-items-center">
        <div class="site-header__name">
          <strong>Miguel Angel</strong><br />Escamilla
        </div>

        <nav class="d-none d-md-flex gap-4">
          <a href="#about" class="site-header__link">{{ t('nav.about') }}</a>
          <a href="#work"  class="site-header__link">{{ t('nav.work') }}</a>
        </nav>

        <LanguageSwitcher />
      </div>
    </div>
  </header>
</template>

<style lang="scss" scoped>
.site-header {
  position: sticky;
  top: 0;
  z-index: 10;
  background: rgba(10, 10, 10, 0.85);
  backdrop-filter: blur(8px);
  border-bottom: 1px solid $border-subtle;

  &__name {
    font-size: 0.875rem;
    line-height: 1.3;
  }

  &__link {
    color: $text-muted;
    text-decoration: none;
    font-size: 0.95rem;

    &:hover { color: $text-primary; }
  }
}
</style>
```

- [ ] **Step 2: Create `app/components/layout/SiteFooter.vue`**

```vue
<script setup lang="ts">
const { t } = useI18n()
const year = new Date().getFullYear()
</script>

<template>
  <footer class="site-footer">
    <div class="container py-4 text-center">
      <small>{{ t('footer.copyright', { year }) }}</small>
    </div>
  </footer>
</template>

<style lang="scss" scoped>
.site-footer {
  border-top: 1px solid $border-subtle;
  color: $text-muted;
  margin-top: 4rem;
}
</style>
```

- [ ] **Step 3: Verify typecheck**

Run: `npm run typecheck`
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add app/components/layout/
git commit -m "feat: add SiteHeader and SiteFooter components"
```

---

## Task 14: Build `HeroSection`

**Files:**
- Create: `app/components/sections/HeroSection.vue`

- [ ] **Step 1: Create `app/components/sections/HeroSection.vue`**

```vue
<script setup lang="ts">
import SocialLink from '~/app/components/ui/SocialLink.vue'
import { socials } from '~/app/content/socials'

const { t } = useI18n()
</script>

<template>
  <section class="hero">
    <div class="container py-5">
      <div class="row align-items-end g-4">
        <div class="col-lg-8">
          <h1 class="display-hero mb-3">{{ t('hero.title') }}</h1>
          <p class="hero__intro">{{ t('hero.intro') }}</p>
        </div>

        <div class="col-lg-4 text-lg-end">
          <a href="#work" class="btn-pill btn-pill--solid">
            {{ t('hero.cta') }}
            <i class="bi bi-arrow-right" aria-hidden="true" />
          </a>
        </div>
      </div>

      <div class="hero__socials mt-5 d-flex flex-wrap gap-3 justify-content-center">
        <SocialLink
          v-for="s in socials"
          :key="s.id"
          :icon="s.icon"
          :url="s.url"
          :label="s.label"
        />
      </div>
    </div>
  </section>
</template>

<style lang="scss" scoped>
.hero {
  padding-block: 4rem;

  &__intro {
    color: $text-muted;
    max-width: 32rem;
    font-size: 1rem;
    margin-bottom: 0;
  }
}
</style>
```

- [ ] **Step 2: Verify typecheck**

Run: `npm run typecheck`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add app/components/sections/HeroSection.vue
git commit -m "feat: add HeroSection with title, intro, CTA, and social links"
```

---

## Task 15: Build `AboutSection`

**Files:**
- Create: `app/components/sections/AboutSection.vue`, `public/images/photo-placeholder.svg`

- [ ] **Step 1: Create `public/images/photo-placeholder.svg`**

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 400 500" preserveAspectRatio="xMidYMid slice">
  <rect width="400" height="500" fill="#1a1a1a"/>
  <circle cx="200" cy="200" r="70" fill="#2a2a2a"/>
  <path d="M 80 500 Q 80 320 200 320 Q 320 320 320 500 Z" fill="#2a2a2a"/>
  <text x="200" y="470" fill="#888" font-family="monospace" font-size="14" text-anchor="middle">photo placeholder</text>
</svg>
```

- [ ] **Step 2: Create `app/components/sections/AboutSection.vue`**

```vue
<script setup lang="ts">
import SkillGroup from '~/app/components/ui/SkillGroup.vue'
import { skillGroups } from '~/app/content/skills'

const { t } = useI18n()
</script>

<template>
  <section id="about" class="about">
    <div class="container py-5">
      <div class="row mb-4 g-4">
        <div class="col-md-4">
          <span class="section-label">{{ t('about.label') }}</span>
        </div>
        <div class="col-md-8">
          <p class="about__intro">{{ t('about.intro') }}</p>
        </div>
      </div>

      <div class="row g-4 align-items-start">
        <div class="col-lg-7 d-flex flex-column gap-3">
          <SkillGroup
            v-for="group in skillGroups"
            :key="group.id"
            :title-key="`about.skillsTitle.${group.id}`"
            :items="group.items"
          />
        </div>

        <div class="col-lg-5">
          <img
            src="/images/photo-placeholder.svg"
            :alt="t('about.photoAlt')"
            class="about__photo img-fluid rounded-4"
          />
        </div>
      </div>
    </div>
  </section>
</template>

<style lang="scss" scoped>
.about {
  padding-block: 4rem;

  &__intro {
    font-size: 1.125rem;
    color: $text-primary;
  }

  &__photo {
    filter: grayscale(100%);
    width: 100%;
    border: 1px solid $border-subtle;
  }
}
</style>
```

- [ ] **Step 3: Verify typecheck**

Run: `npm run typecheck`
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add app/components/sections/AboutSection.vue public/images/photo-placeholder.svg
git commit -m "feat: add AboutSection with skill groups and photo placeholder"
```

---

## Task 16: Build `WorkSection` (TDD)

**Files:**
- Test: `tests/components/WorkSection.test.ts`
- Create: `app/components/sections/WorkSection.vue`

- [ ] **Step 1: Write failing test**

Create `tests/components/WorkSection.test.ts`:

```ts
import { describe, it, expect, vi } from 'vitest'
import { mount } from '@vue/test-utils'
import WorkSection from '~/app/components/sections/WorkSection.vue'

vi.mock('~/app/content/work', () => ({
  jobs: [
    { id: 'job-1', start: '2024-01', end: 'present', company: 'Acme',  roleKey: 'work.jobs.job-1.role' },
    { id: 'job-2', start: '2022-06', end: '2023-12', company: 'Globex', roleKey: 'work.jobs.job-2.role' },
  ],
}))

describe('WorkSection', () => {
  it('renders one row per job with company and translated role', () => {
    const wrapper = mount(WorkSection)

    const rows = wrapper.findAll('tr')
    expect(rows.length).toBe(2)

    expect(wrapper.text()).toContain('Acme')
    expect(wrapper.text()).toContain('Globex')
    expect(wrapper.text()).toContain('work.jobs.job-1.role')
    expect(wrapper.text()).toContain('work.jobs.job-2.role')
  })

  it('renders the section title and total experience label', () => {
    const wrapper = mount(WorkSection)
    expect(wrapper.text()).toContain('work.title')
    expect(wrapper.text()).toContain('work.totalLabel')
  })
})
```

- [ ] **Step 2: Run test — verify it fails**

Run: `npm test`
Expected: FAIL — `WorkSection` not found.

- [ ] **Step 3: Create `app/components/sections/WorkSection.vue`**

```vue
<script setup lang="ts">
import { jobs } from '~/app/content/work'
import { calculateExperience } from '~/app/utils/experience'

const { t } = useI18n()
const total = calculateExperience(jobs)

function formatRange(start: string, end: string | 'present'): string {
  const startYear = start.slice(0, 4)
  if (end === 'present') return `${startYear} - ${t('work.present')}`
  const endYear = end.slice(0, 4)
  return startYear === endYear ? startYear : `${startYear} - ${endYear}`
}

function durationFor(start: string, end: string | 'present'): string {
  const single = calculateExperience(
    [{ id: 'tmp', start, end, company: '', roleKey: '' }],
  )
  return t('work.duration', { years: single.years, months: single.months })
}
</script>

<template>
  <section id="work" class="work">
    <div class="container py-5">
      <h2 class="display-section text-end mb-4">{{ t('work.title') }}</h2>

      <table class="work-table">
        <tbody>
          <tr v-for="job in jobs" :key="job.id">
            <td>
              <div>{{ formatRange(job.start, job.end) }}</div>
              <small class="text-muted">{{ durationFor(job.start, job.end) }}</small>
            </td>
            <td>{{ job.company }}</td>
            <td>{{ t(job.roleKey) }}</td>
          </tr>
        </tbody>
      </table>

      <div class="text-end mt-3 work__total">
        <div>{{ t('work.totalLabel') }}</div>
        <strong>{{ t('work.yearsMonths', { years: total.years, months: total.months }) }}</strong>
      </div>
    </div>
  </section>
</template>

<style lang="scss" scoped>
.work {
  padding-block: 4rem;

  &__total {
    color: $text-muted;
    font-size: 0.95rem;
    strong { color: $text-primary; display: block; }
  }
}
</style>
```

- [ ] **Step 4: Run tests — verify they pass**

Run: `npm test`
Expected: PASS — both `WorkSection` tests green.

- [ ] **Step 5: Commit**

```bash
git add app/components/sections/WorkSection.vue tests/components/WorkSection.test.ts
git commit -m "feat: add WorkSection with job table and experience total"
```

---

## Task 17: Assemble the page

**Files:**
- Modify: `app/pages/index.vue`, `app/app.vue`

- [ ] **Step 1: Update `app/pages/index.vue`**

```vue
<script setup lang="ts">
import SiteHeader from '~/app/components/layout/SiteHeader.vue'
import SiteFooter from '~/app/components/layout/SiteFooter.vue'
import HeroSection from '~/app/components/sections/HeroSection.vue'
import AboutSection from '~/app/components/sections/AboutSection.vue'
import WorkSection from '~/app/components/sections/WorkSection.vue'

const { t, locale } = useI18n()

useHead({
  title: () => `Miguel Angel Escamilla — ${t('hero.title')}`,
  htmlAttrs: { lang: locale.value },
})
</script>

<template>
  <div class="page">
    <SiteHeader />
    <main>
      <HeroSection />
      <AboutSection />
      <WorkSection />
    </main>
    <SiteFooter />
  </div>
</template>
```

- [ ] **Step 2: Verify `app/app.vue` is the correct minimal wrapper**

`app/app.vue` should already be:

```vue
<template>
  <NuxtPage />
</template>
```

If it isn't, replace it with the snippet above.

- [ ] **Step 3: Run dev server and visually inspect**

Run: `npm run dev`. Open `http://localhost:3000/es` in a browser. Verify:
- Header shows "Miguel Angel Escamilla", nav links "Sobre mí" / "Experiencia", and Es/En switcher.
- Hero shows large "Full-stack Developer", intro paragraph, "Proyectos" pill button, and 4 social links.
- About section shows label "/Sobre mí", intro paragraph, 4 skill groups, and photo placeholder.
- Work section shows large "Experiencia" title, table with 2 jobs, and total label.
- Clicking the En switcher navigates to `/en` and all text changes to English.
- Clicking nav "Sobre mí"/"Experiencia" smooth-scrolls to the corresponding section.
- Page is dark with white text; layout collapses to single column on mobile (use DevTools responsive mode at 375px width).

Ctrl-C to stop. If any of the above fails, debug before continuing.

- [ ] **Step 4: Run all tests**

Run: `npm test`
Expected: PASS — all tests across the 4 test files still green.

- [ ] **Step 5: Run typecheck**

Run: `npm run typecheck`
Expected: no errors.

- [ ] **Step 6: Commit**

```bash
git add app/pages/index.vue app/app.vue
git commit -m "feat: assemble portfolio page with all sections"
```

---

## Task 18: Configure SSG build for GitHub Pages

**Files:**
- Modify: `nuxt.config.ts`

- [ ] **Step 1: Add nitro preset to `nuxt.config.ts`**

Add a `nitro` block to the `defineNuxtConfig` object (alongside `i18n`, `app`, etc.):

```ts
nitro: {
  preset: 'github-pages',
  prerender: {
    crawlLinks: true,
    routes: ['/'],
  },
},
```

The full updated `nuxt.config.ts`:

```ts
export default defineNuxtConfig({
  compatibilityDate: '2026-05-08',
  devtools: { enabled: true },
  future: { compatibilityVersion: 4 },
  typescript: { strict: true },
  modules: ['@nuxtjs/i18n'],
  css: ['~/assets/scss/main.scss'],
  vite: {
    css: {
      preprocessorOptions: {
        scss: {
          additionalData: '@use "~/assets/scss/variables" as *;',
        },
      },
    },
  },
  i18n: {
    strategy: 'prefix',
    defaultLocale: 'es',
    locales: [
      { code: 'es', name: 'Español', file: 'es.json' },
      { code: 'en', name: 'English', file: 'en.json' },
    ],
    detectBrowserLanguage: false,
  },
  nitro: {
    preset: 'github-pages',
    prerender: {
      crawlLinks: true,
      routes: ['/'],
    },
  },
  app: {
    baseURL: '/',
    head: {
      title: 'Miguel Angel Escamilla — Full-stack Developer',
      htmlAttrs: { lang: 'es' },
      meta: [
        { name: 'viewport', content: 'width=device-width, initial-scale=1' },
        { name: 'description', content: 'Full-stack developer portfolio.' },
      ],
      link: [
        { rel: 'preconnect', href: 'https://fonts.googleapis.com' },
        { rel: 'preconnect', href: 'https://fonts.gstatic.com', crossorigin: '' },
        {
          rel: 'stylesheet',
          href: 'https://fonts.googleapis.com/css2?family=Archivo+Black&family=Inter:wght@400;500;600;700&display=swap',
        },
      ],
    },
  },
})
```

- [ ] **Step 2: Run static build**

Run: `npm run generate`
Expected: succeeds; `.output/public/` contains `es/index.html`, `en/index.html`, `index.html`, `404.html`, `.nojekyll`, and `_nuxt/` assets.

- [ ] **Step 3: Verify the static build locally**

Run: `npx serve .output/public -p 5000`
Open `http://localhost:5000` — should redirect/render the Spanish version. Visit `http://localhost:5000/en` for English. Ctrl-C to stop.

- [ ] **Step 4: Commit**

```bash
git add nuxt.config.ts
git commit -m "build: configure nitro github-pages preset for SSG"
```

---

## Task 19: Add GitHub Actions deployment workflow

**Files:**
- Create: `.github/workflows/deploy.yml`

- [ ] **Step 1: Create `.github/workflows/deploy.yml`**

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Run tests
        run: npm test

      - name: Build static site
        run: npm run generate

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .output/public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: add GitHub Actions workflow for Pages deploy"
```

---

## Task 20: Add favicon and README

**Files:**
- Create: `public/favicon.svg`, `README.md`

- [ ] **Step 1: Create `public/favicon.svg`**

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="6" fill="#0a0a0a"/>
  <text x="16" y="22" font-family="Archivo Black, system-ui, sans-serif" font-weight="900" font-size="18" fill="#ffffff" text-anchor="middle">M</text>
</svg>
```

- [ ] **Step 2: Wire favicon in `nuxt.config.ts`**

Add a favicon entry to the `app.head.link` array (after the Google Fonts entries):

```ts
{ rel: 'icon', type: 'image/svg+xml', href: '/favicon.svg' },
```

- [ ] **Step 3: Create `README.md`**

```markdown
# my-portfolio

Personal portfolio of Miguel Angel Escamilla — full-stack developer.

**Live:** https://mig21010.github.io/

## Stack
- Nuxt 4 + Vue 3.5 + TypeScript
- Bootstrap 5 SCSS (partial) + Bootstrap Icons
- `@nuxtjs/i18n` (ES / EN)
- Vitest + Vue Test Utils

## Local development

```bash
npm install
npm run dev          # http://localhost:3000
```

## Tests and typecheck

```bash
npm test
npm run typecheck
```

## Static build

```bash
npm run generate          # outputs .output/public
npx serve .output/public  # preview locally
```

## Deploy

Pushed commits to `main` are built and published to GitHub Pages by `.github/workflows/deploy.yml`.
Enable Pages in the repo settings (Source: GitHub Actions) before the first run.

## Editing content

- Personal text per language: `i18n/locales/es.json`, `i18n/locales/en.json`
- Social links: `app/content/socials.ts`
- Skill groups: `app/content/skills.ts`
- Work experience entries: `app/content/work.ts`
```

- [ ] **Step 4: Verify build still passes**

Run: `npm run generate`
Expected: succeeds; `.output/public/favicon.svg` exists.

- [ ] **Step 5: Commit**

```bash
git add public/favicon.svg nuxt.config.ts README.md
git commit -m "docs: add favicon and README"
```

---

## Task 21: Final verification

- [ ] **Step 1: Run full test suite**

Run: `npm test`
Expected: PASS — all tests across `experience`, `SkillGroup`, `LanguageSwitcher`, `WorkSection`.

- [ ] **Step 2: Run typecheck**

Run: `npm run typecheck`
Expected: no errors.

- [ ] **Step 3: Run static build**

Run: `npm run generate`
Expected: succeeds; output contains `.nojekyll`, `404.html`, `index.html`, `es/index.html`, `en/index.html`.

- [ ] **Step 4: Manual smoke test in browser**

Run: `npx serve .output/public -p 5000`. In a browser:
- Visit `/` — renders ES content (auto-redirect from default locale).
- Visit `/en` — renders EN content.
- Click language switcher — navigates to other locale, content swaps.
- Click "Sobre mí" / "About" nav link — smooth-scrolls to About section.
- Click "Experiencia" / "Work" — smooth-scrolls to Work section.
- All 4 social links open in new tabs (`target="_blank"`).
- Resize to mobile width (375px) — layout collapses to single column, no horizontal overflow.

Ctrl-C to stop.

- [ ] **Step 5: Done**

The portfolio is ready. To deploy:
1. Create the GitHub repo `mig21010.github.io` (public).
2. `git remote add origin git@github.com:mig21010/mig21010.github.io.git`
3. `git push -u origin main`
4. In GitHub repo settings → Pages → Source: **GitHub Actions**.
5. The workflow runs automatically on push; live at `https://mig21010.github.io/` within ~2 minutes.
